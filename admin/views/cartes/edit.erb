<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:cartes, :index) %></li>
      <li><%= link_to pat(:new), url(:cartes, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:cartes, :edit, :id => @carte.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:carte) %></h2>
    <div class="inner">
      <% form_for :carte, url(:cartes, :update, :id => @carte.id), :method => :put, :class => :form, :multipart => true do |f| %>
        <%= partial "cartes/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
